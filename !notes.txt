# 25SEP2019

default behavior:
    generate file in .m3u format for amt
    default table columns: source, group, pps
    optional columns: amt URI, reporting router



????
does the VERBOSE = true loop (currently @ line 88) print all the values, including anything with PPS < 5?
does line 78 (if s.pps > 5:) limit the initial output?


*** need to sleep in loop to prevent
rc.text
Out[16]: 'Please wait 1 seconds before sending another command.'
(probably also need to test and sleep longer if this happens)


create list of dicts --> create panda frame


# remove dups
a.

# sorting the dataframe
b = a.sort_values(by=['pps', 'packets', 'source', 'router'], ascending=False)


# interesting columns
c = b.filter(['group', 'source', 'pps'])

# ignore dups
c.drop_duplicates()


